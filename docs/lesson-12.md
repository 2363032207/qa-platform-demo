# 第 12 课笔记（请用自己的话补全）

## 1. 性能基线和「看一眼 PerfDog 报告」有什么区别？

可以告诉我哪些场景下不达标,perfDog需要人力去分析

## 2. 为什么要按「场景 + 机型」定基线，而不是全游戏一个 FPS 数？

不同场景下，不同的机型性能不一样

## 3. 平台里的性能 FAIL，和功能门禁 FAIL，处理方式有何不同？

## 4. 本次实操：贴一次 PASS 与一次 FAIL 的结论（Swagger / 看板均可）

```text
（PASS）
PASS
场景 lobby 性能达标（相对基线）
（FAIL）
FAIL
场景 combat_dense 未达基线：avg_fps, min_fps, frame_time_p95_ms
```

